interface:
  display_name: "Local Scientific Compute"
  short_description: "Run reproducible science on the local computer"
  default_prompt: "Use $compute-environment to run this scientific workload locally and record reproducible outputs."
