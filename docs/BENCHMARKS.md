# Benchmarks

No performance benchmark is defined for the plugin. Regression gates are deterministic unit/integration tests, inventory reproducibility, plugin validation, and the separate live public-source smoke check.
