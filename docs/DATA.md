# Data

This repository contains no research dataset. Imported skill instructions live in the pinned `vendor/` submodule. Public connector metadata lives in `connectors/public.json`.

User research data remains in the user's project. Do not copy sensitive raw data into this repository or an artifact bundle; record an approved path and hash instead.
