interface:
  display_name: "Kdense What If Oracle"
  short_description: "Use the audited kdense-what-if-oracle scientific workflow"
  default_prompt: "Use $kdense-what-if-oracle for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
