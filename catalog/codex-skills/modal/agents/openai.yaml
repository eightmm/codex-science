interface:
  display_name: "Modal"
  short_description: "Use the audited modal scientific workflow"
  default_prompt: "Use $modal for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
