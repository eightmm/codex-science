interface:
  display_name: "Matchms"
  short_description: "Use the audited matchms scientific workflow"
  default_prompt: "Use $matchms for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
