interface:
  display_name: "Sympy"
  short_description: "Use the audited sympy scientific workflow"
  default_prompt: "Use $sympy for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
