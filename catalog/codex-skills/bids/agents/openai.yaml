interface:
  display_name: "Bids"
  short_description: "Use the audited bids scientific workflow"
  default_prompt: "Use $bids for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
