interface:
  display_name: "Tamarind"
  short_description: "Use the audited tamarind scientific workflow"
  default_prompt: "Use $tamarind for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
