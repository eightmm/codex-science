interface:
  display_name: "Parallel Web"
  short_description: "Use the audited parallel-web scientific workflow"
  default_prompt: "Use $parallel-web for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
