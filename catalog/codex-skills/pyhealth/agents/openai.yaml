interface:
  display_name: "Pyhealth"
  short_description: "Use the audited pyhealth scientific workflow"
  default_prompt: "Use $pyhealth for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
