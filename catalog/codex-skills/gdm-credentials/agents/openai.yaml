interface:
  display_name: "Gdm Credentials"
  short_description: "Use the audited gdm-credentials scientific workflow"
  default_prompt: "Use $gdm-credentials for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
