interface:
  display_name: "Pptx"
  short_description: "Use the audited pptx scientific workflow"
  default_prompt: "Use $pptx for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
