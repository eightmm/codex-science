interface:
  display_name: "Pi Agent"
  short_description: "Use the audited pi-agent scientific workflow"
  default_prompt: "Use $pi-agent for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
