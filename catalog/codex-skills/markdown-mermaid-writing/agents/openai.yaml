interface:
  display_name: "Markdown Mermaid Writing"
  short_description: "Use the audited markdown-mermaid-writing scientific workflow"
  default_prompt: "Use $markdown-mermaid-writing for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
