interface:
  display_name: "Cx Compute Environment"
  short_description: "Use the audited cx-compute-environment scientific workflow"
  default_prompt: "Use $cx-compute-environment for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
