interface:
  display_name: "Depmap"
  short_description: "Use the audited depmap scientific workflow"
  default_prompt: "Use $depmap for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
