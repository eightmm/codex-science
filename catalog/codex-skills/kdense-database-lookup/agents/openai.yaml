interface:
  display_name: "Kdense Database Lookup"
  short_description: "Use the audited kdense-database-lookup scientific workflow"
  default_prompt: "Use $kdense-database-lookup for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
