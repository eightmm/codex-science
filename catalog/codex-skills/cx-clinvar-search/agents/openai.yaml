interface:
  display_name: "Cx Clinvar Search"
  short_description: "Use the audited cx-clinvar-search scientific workflow"
  default_prompt: "Use $cx-clinvar-search for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
