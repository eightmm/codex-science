interface:
  display_name: "Cx Ncbi Sequence Fetch"
  short_description: "Use the audited cx-ncbi-sequence-fetch scientific workflow"
  default_prompt: "Use $cx-ncbi-sequence-fetch for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
