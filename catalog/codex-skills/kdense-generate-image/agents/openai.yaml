interface:
  display_name: "Kdense Generate Image"
  short_description: "Use the audited kdense-generate-image scientific workflow"
  default_prompt: "Use $kdense-generate-image for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
