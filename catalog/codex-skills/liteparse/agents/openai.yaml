interface:
  display_name: "Liteparse"
  short_description: "Use the audited liteparse scientific workflow"
  default_prompt: "Use $liteparse for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
