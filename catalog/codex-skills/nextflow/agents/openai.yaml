interface:
  display_name: "Nextflow"
  short_description: "Use the audited nextflow scientific workflow"
  default_prompt: "Use $nextflow for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
