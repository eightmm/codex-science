interface:
  display_name: "Primekg"
  short_description: "Use the audited primekg scientific workflow"
  default_prompt: "Use $primekg for this scientific task with catalog safeguards."
policy:
  allow_implicit_invocation: false
